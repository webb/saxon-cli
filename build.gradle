apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    // https://mvnrepository.com/artifact/net.sf.saxon/Saxon-HE
    runtime group: 'net.sf.saxon', name: 'Saxon-HE', version: '10.1'
}

task save_jars(type:Copy) {
     from configurations.runtime
     into "${project.jars_dir}"
}
