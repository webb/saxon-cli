saxon-cli: A command-line wrapper for the Saxon HE XSLT processor.

Primary Author:

Webb Roberts
email:   mailto:webb.roberts@gtri.gatech.edu
phone:   (404)407-6181
github:  https://github.com/webb
twitter: @webbr

See COPYING for info about the license under which this software is released.

See INSTALL for instructions on configuring, building, and installing this
software.

Installation summary:
  ./configure --prefix=${installation root directory}
  make
  make install

If you're installing using stow:
  ./configure --prefix=${installation root directory}
  make
  make -f stow.mk

If you want to rebuild configure:
  make -f unconfigured.mk init


